import React from 'react';

const landing = () => {

    return (
        <section>
        <button>Log in</button>
        <button>Create Account</button>
        </section>
    )
}

export default landing;